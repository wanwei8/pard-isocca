
    var hasAddAuth = [[${#authorization.expression('hasAuthority("sys:dict:add")')}]];
    var hasEditAuth = [[${#authorization.expression('hasAuthority("sys:dict:edit")')}]];
    var hasDelAuth = [[${#authorization.expression('hasAuthority("sys:dict:del")')}]];
    var hasViewAuth = [[${#authorization.expression('hasAuthority("sys:dict:view")')}]];